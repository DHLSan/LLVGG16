// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4.1
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

/***************************** Include Files *********************************/
#include "xdense_reluflatten_softmax.h"

/************************** Function Implementation *************************/
#ifndef __linux__
int XDense_reluflatten_softmax_CfgInitialize(XDense_reluflatten_softmax *InstancePtr, XDense_reluflatten_softmax_Config *ConfigPtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(ConfigPtr != NULL);

    InstancePtr->Control_BaseAddress = ConfigPtr->Control_BaseAddress;
    InstancePtr->Input_BaseAddress = ConfigPtr->Input_BaseAddress;
    InstancePtr->Output_BaseAddress = ConfigPtr->Output_BaseAddress;
    InstancePtr->Parameter_BaseAddress = ConfigPtr->Parameter_BaseAddress;
    InstancePtr->IsReady = XIL_COMPONENT_IS_READY;

    return XST_SUCCESS;
}
#endif

void XDense_reluflatten_softmax_Start(XDense_reluflatten_softmax *InstancePtr) {
    u32 Data;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XDense_reluflatten_softmax_ReadReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_AP_CTRL) & 0x80;
    XDense_reluflatten_softmax_WriteReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_AP_CTRL, Data | 0x01);
}

u32 XDense_reluflatten_softmax_IsDone(XDense_reluflatten_softmax *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XDense_reluflatten_softmax_ReadReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_AP_CTRL);
    return (Data >> 1) & 0x1;
}

u32 XDense_reluflatten_softmax_IsIdle(XDense_reluflatten_softmax *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XDense_reluflatten_softmax_ReadReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_AP_CTRL);
    return (Data >> 2) & 0x1;
}

u32 XDense_reluflatten_softmax_IsReady(XDense_reluflatten_softmax *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XDense_reluflatten_softmax_ReadReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_AP_CTRL);
    // check ap_start to see if the pcore is ready for next input
    return !(Data & 0x1);
}

void XDense_reluflatten_softmax_EnableAutoRestart(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XDense_reluflatten_softmax_WriteReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_AP_CTRL, 0x80);
}

void XDense_reluflatten_softmax_DisableAutoRestart(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XDense_reluflatten_softmax_WriteReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_AP_CTRL, 0);
}

u32 XDense_reluflatten_softmax_Get_mem_block1_dense_BaseAddress(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Input_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_BASE);
}

u32 XDense_reluflatten_softmax_Get_mem_block1_dense_HighAddress(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Input_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_HIGH);
}

u32 XDense_reluflatten_softmax_Get_mem_block1_dense_TotalBytes(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_BASE + 1);
}

u32 XDense_reluflatten_softmax_Get_mem_block1_dense_BitWidth(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XDENSE_RELUFLATTEN_SOFTMAX_INPUT_WIDTH_MEM_BLOCK1_DENSE;
}

u32 XDense_reluflatten_softmax_Get_mem_block1_dense_Depth(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XDENSE_RELUFLATTEN_SOFTMAX_INPUT_DEPTH_MEM_BLOCK1_DENSE;
}

u32 XDense_reluflatten_softmax_Write_mem_block1_dense_Words(XDense_reluflatten_softmax *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Input_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Read_mem_block1_dense_Words(XDense_reluflatten_softmax *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Input_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_BASE + (offset + i)*4);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Write_mem_block1_dense_Bytes(XDense_reluflatten_softmax *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Input_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Read_mem_block1_dense_Bytes(XDense_reluflatten_softmax *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Input_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_INPUT_ADDR_MEM_BLOCK1_DENSE_BASE + offset + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Get_class_BaseAddress(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Output_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_BASE);
}

u32 XDense_reluflatten_softmax_Get_class_HighAddress(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Output_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_HIGH);
}

u32 XDense_reluflatten_softmax_Get_class_TotalBytes(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_BASE + 1);
}

u32 XDense_reluflatten_softmax_Get_class_BitWidth(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_WIDTH_CLASS;
}

u32 XDense_reluflatten_softmax_Get_class_Depth(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_DEPTH_CLASS;
}

u32 XDense_reluflatten_softmax_Write_class_Words(XDense_reluflatten_softmax *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Output_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Read_class_Words(XDense_reluflatten_softmax *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Output_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_BASE + (offset + i)*4);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Write_class_Bytes(XDense_reluflatten_softmax *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Output_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Read_class_Bytes(XDense_reluflatten_softmax *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Output_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_CLASS_BASE + offset + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Get_result_BaseAddress(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Output_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_BASE);
}

u32 XDense_reluflatten_softmax_Get_result_HighAddress(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Output_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_HIGH);
}

u32 XDense_reluflatten_softmax_Get_result_TotalBytes(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_BASE + 1);
}

u32 XDense_reluflatten_softmax_Get_result_BitWidth(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_WIDTH_RESULT;
}

u32 XDense_reluflatten_softmax_Get_result_Depth(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_DEPTH_RESULT;
}

u32 XDense_reluflatten_softmax_Write_result_Words(XDense_reluflatten_softmax *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Output_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Read_result_Words(XDense_reluflatten_softmax *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Output_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_BASE + (offset + i)*4);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Write_result_Bytes(XDense_reluflatten_softmax *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Output_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Read_result_Bytes(XDense_reluflatten_softmax *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Output_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_OUTPUT_ADDR_RESULT_BASE + offset + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Get_bd1_BaseAddress(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_BASE);
}

u32 XDense_reluflatten_softmax_Get_bd1_HighAddress(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_HIGH);
}

u32 XDense_reluflatten_softmax_Get_bd1_TotalBytes(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_BASE + 1);
}

u32 XDense_reluflatten_softmax_Get_bd1_BitWidth(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_WIDTH_BD1;
}

u32 XDense_reluflatten_softmax_Get_bd1_Depth(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_DEPTH_BD1;
}

u32 XDense_reluflatten_softmax_Write_bd1_Words(XDense_reluflatten_softmax *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Read_bd1_Words(XDense_reluflatten_softmax *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_BASE + (offset + i)*4);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Write_bd1_Bytes(XDense_reluflatten_softmax *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Read_bd1_Bytes(XDense_reluflatten_softmax *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD1_BASE + offset + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Get_bd2_BaseAddress(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_BASE);
}

u32 XDense_reluflatten_softmax_Get_bd2_HighAddress(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_HIGH);
}

u32 XDense_reluflatten_softmax_Get_bd2_TotalBytes(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_BASE + 1);
}

u32 XDense_reluflatten_softmax_Get_bd2_BitWidth(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_WIDTH_BD2;
}

u32 XDense_reluflatten_softmax_Get_bd2_Depth(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_DEPTH_BD2;
}

u32 XDense_reluflatten_softmax_Write_bd2_Words(XDense_reluflatten_softmax *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Read_bd2_Words(XDense_reluflatten_softmax *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_BASE + (offset + i)*4);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Write_bd2_Bytes(XDense_reluflatten_softmax *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Read_bd2_Bytes(XDense_reluflatten_softmax *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD2_BASE + offset + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Get_bd3_BaseAddress(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_BASE);
}

u32 XDense_reluflatten_softmax_Get_bd3_HighAddress(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_HIGH);
}

u32 XDense_reluflatten_softmax_Get_bd3_TotalBytes(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_BASE + 1);
}

u32 XDense_reluflatten_softmax_Get_bd3_BitWidth(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_WIDTH_BD3;
}

u32 XDense_reluflatten_softmax_Get_bd3_Depth(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_DEPTH_BD3;
}

u32 XDense_reluflatten_softmax_Write_bd3_Words(XDense_reluflatten_softmax *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Read_bd3_Words(XDense_reluflatten_softmax *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_BASE + (offset + i)*4);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Write_bd3_Bytes(XDense_reluflatten_softmax *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XDense_reluflatten_softmax_Read_bd3_Bytes(XDense_reluflatten_softmax *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_HIGH - XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Parameter_BaseAddress + XDENSE_RELUFLATTEN_SOFTMAX_PARAMETER_ADDR_BD3_BASE + offset + i);
    }
    return length;
}

void XDense_reluflatten_softmax_InterruptGlobalEnable(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XDense_reluflatten_softmax_WriteReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_GIE, 1);
}

void XDense_reluflatten_softmax_InterruptGlobalDisable(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XDense_reluflatten_softmax_WriteReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_GIE, 0);
}

void XDense_reluflatten_softmax_InterruptEnable(XDense_reluflatten_softmax *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XDense_reluflatten_softmax_ReadReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_IER);
    XDense_reluflatten_softmax_WriteReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_IER, Register | Mask);
}

void XDense_reluflatten_softmax_InterruptDisable(XDense_reluflatten_softmax *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XDense_reluflatten_softmax_ReadReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_IER);
    XDense_reluflatten_softmax_WriteReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_IER, Register & (~Mask));
}

void XDense_reluflatten_softmax_InterruptClear(XDense_reluflatten_softmax *InstancePtr, u32 Mask) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XDense_reluflatten_softmax_WriteReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_ISR, Mask);
}

u32 XDense_reluflatten_softmax_InterruptGetEnabled(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XDense_reluflatten_softmax_ReadReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_IER);
}

u32 XDense_reluflatten_softmax_InterruptGetStatus(XDense_reluflatten_softmax *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XDense_reluflatten_softmax_ReadReg(InstancePtr->Control_BaseAddress, XDENSE_RELUFLATTEN_SOFTMAX_CONTROL_ADDR_ISR);
}

